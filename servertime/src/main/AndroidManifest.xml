<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.philips.cl.di.servertime">

    <application android:allowBackup="true" android:label="@string/app_name">

        <receiver android:name="com.philips.cdp.recievers.DateTimeChangedReceiver" >
            <intent-filter android:priority="100">
                <action android:name="android.intent.action.DATE_CHANGED"></action>
                <action android:name="android.intent.action.TIME_SET"></action>
            </intent-filter>
        </receiver>

    </application>

</manifest>

package com.philips.platform.modularui.statecontroller;

import android.content.Context;

/**
 * Interface to have callbacks to the registered class.
 */
public interface CoCoListener {
    void coCoCallBack(Context context);
}

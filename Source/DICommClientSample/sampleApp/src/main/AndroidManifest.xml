<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.philips.cdp.dicommclientsample"
    android:versionCode="1"
    android:versionName="1.0.0">

    <uses-sdk tools:overrideLibrary="com.philips.cdp.registration, com.philips.dhpclient, com.philips.cl.di.servertime, com.janrain.android, com.philips.platform.appinfra, com.philips.cdp.prxclient, com.philips.cdp.localematch"/>

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <application
        android:allowBackup="false"
        tools:replace="android:allowBackup"
        android:icon="@drawable/ic_dicomm_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:name=".App" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DetailActivity" />
    </application>

</manifest>

configurations.maybeCreate("default")

def bluelibAar = rootDir.absolutePath + '/../../../android-shinelib/Source/ShineLib/shinelib/build/outputs/aar/shinelib-debug.aar'
println(bluelibAar)

if (new File(bluelibAar).exists()) {
    System.out.println(">>> adding shinelib-debug.aar to artifacts")
    artifacts.add("default", file(bluelibAar))
}

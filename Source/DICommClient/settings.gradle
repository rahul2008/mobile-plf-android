rootProject.setName 'dicommClientLib'
include ':dicommClientLib'

def commonDir = '../common'
if (new File(commonDir).exists()) {
    include ':common'
    project(':common').projectDir = new File(commonDir)
}

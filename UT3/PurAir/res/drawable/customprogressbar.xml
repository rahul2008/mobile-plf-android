<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
  
<!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
	    <corners
                    android:radius="5dp" />

            <gradient                
                    android:startColor="@color/progressbar_gray2"
                    android:centerColor="@color/progressbar_gray2"
                    android:centerY="1.0"
                    android:endColor="@color/progressbar_gray2"
                    android:angle="270"
            />
        </shape>
    </item>

 <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
		<corners
                    android:radius="5dp" />

                <gradient
                    android:startColor="@color/philips_blue" 
                    android:centerColor="@color/philips_blue" 
                    android:centerY="1.0"
                    android:endColor="@color/philips_blue" 
                    android:angle="270"
                />
            </shape>
        </clip>
    </item>

</layer-list>

